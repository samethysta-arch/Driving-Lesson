"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { visualQuestions } from "../questions";
import "../btt.css";
export default function VisualQuiz() { const [index, setIndex] = useState(0); const [choice, setChoice] = useState<number | null>(null); const [submitted, setSubmitted] = useState(false); const [answers, setAnswers] = useState<Record<string, boolean>>({}); const q = visualQuestions[index]; const reveal = submitted; useEffect(() => { const saved = JSON.parse(localStorage.getItem("btt-visual-v1") || "{}") as Record<string, boolean>; setAnswers(saved); }, []); const submit = () => { if (choice === null) return; const next = { ...answers, [q.id]: choice === q.answer }; setAnswers(next); setSubmitted(true); localStorage.setItem("btt-visual-v1", JSON.stringify(next)); }; return <main className="btt-shell"><header className="btt-header"><Link href="/btt" className="home-link">← BTT practice</Link><strong>Visual Signage Quiz</strong><span>{Object.values(answers).filter(Boolean).length}/{visualQuestions.length} correct</span></header><section className="quiz-wrap"><div className="quiz-meta"><span>Visual question</span><span>Question {index + 1} of {visualQuestions.length}</span><span>Tap image to zoom</span></div><div className="progress"><span style={{ width: `${(index / visualQuestions.length) * 100}%` }} /></div><article className="quiz-card visual-card"><button className="visual-frame" onClick={() => window.open(q.image, "_blank")} aria-label="Open a larger version of the traffic sign or road visual"><img src={q.image} alt="Traffic sign, road marking or signal to identify" /></button><h1>{q.prompt}</h1><div className="choices">{q.options.map((option, i) => <button disabled={reveal} key={option} className={reveal ? i === q.answer ? "correct" : i === choice ? "incorrect" : "" : choice === i ? "selected" : ""} onClick={() => setChoice(i)}><span>{"ABCD"[i]}</span>{option}</button>)}</div>{!reveal ? <button className="submit" disabled={choice === null} onClick={submit}>Submit answer</button> : <div className="reveal"><b>{answers[q.id] ? "Correct" : "Not quite"}</b><p>{q.explanation}</p><p className="reference">Handbook p.{q.page} · {q.topic}</p><div className="next-row"><button disabled={index === 0} onClick={() => { setIndex(index - 1); setChoice(null); setSubmitted(false); }}>Previous</button><button disabled={index === visualQuestions.length - 1} onClick={() => { setIndex(index + 1); setChoice(null); setSubmitted(false); }}>Next question →</button></div></div>}</article></section></main>; }
